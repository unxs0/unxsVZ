#
#iDNS tHit subsystem related
23 3 * * * /usr/sbin/collector.sh > /tmp/collector.log 2>&1

#
#iDNS querylog subsystem items
#
#       After nodes use their collector.sh to send daily query hit tables we
#       then
#       aggregate them into the current tHit table per tZone.cZone.
10 4 * * * /usr/sbin/aggregator.sh > /tmp/aggregator.log 2>&1

#
#Please not the timing for collection and aggregation and possible backup
#activities.

#
#Archive special iDNS data sets
1 0 1 * *  /cgi-bin/iDNS.cgi ExtracttLog `/bin/date +"%b %Y"` Yh57fk 
/var/lib/mysql/idns > /tmp/ExtracttLog.log 2>&1
#
30 6 1 * *  /cgi-bin/iDNS.cgi ExtracttHit `/bin/date +"%b %Y"` 0084YhgT 
/var/lib/mysql/idns > /tmp/ExtracttHit.log 2>&1

