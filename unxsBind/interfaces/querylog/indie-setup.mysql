#
#For new mysqld
GRANT ALL ON *.* TO root@localhost IDENTIFIED BY 'ZorBexin';

CREATE DATABASE idns;
GRANT ALL ON idns.* TO idns@localhost IDENTIFIED BY 'wsxedc';

USE idns;
CREATE TABLE IF NOT EXISTS tHit ( uHit INT UNSIGNED PRIMARY KEY AUTO_INCREMENT, cZone VARCHAR(100) NOT NULL DEFAULT '',unique (cZone), uOwner INT UNSIGNED NOT NULL DEFAULT 0,index (uOwner), uCreatedBy INT UNSIGNED NOT NULL DEFAULT 0, uCreatedDate INT UNSIGNED NOT NULL DEFAULT 0, uModBy INT UNSIGNED NOT NULL DEFAULT 0, uModDate INT UNSIGNED NOT NULL DEFAULT 0, uHitCount INT UNSIGNED NOT NULL DEFAULT 0 );
